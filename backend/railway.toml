[build]
builder = "DOCKERFILE"
buildCommand = "docker build -t recolekt-backend ."

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:${PORT:-8080} --workers 1 --threads 4 --timeout 180 --chdir /app/api-flask app:app"
